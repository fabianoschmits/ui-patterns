"use client";
import { useState } from "react";
import { ChevronDown, ChevronRight, FileText, Folder, Grid2X2, Layers, Settings } from "lucide-react";
import type { PatternPreviewProps } from "@/types/pattern";
import { PreviewFrame } from "@/patterns/shared/preview-frame";
export default function NestedSidebar(props: PatternPreviewProps) { const [open, setOpen] = useState(true); const [active, setActive] = useState("Overview"); return <PreviewFrame {...props} className="nested-demo"><aside className="nested-side"><div className="nested-title"><span className="nested-logo">N</span><b>nova / workspace</b><ChevronDown size={13} /></div><small className="nested-label">WORKSPACE</small><button className={active === "Overview" ? "active" : ""} onClick={() => setActive("Overview")}><Grid2X2 size={15} />Overview</button><button onClick={() => setOpen((v) => !v)}><Layers size={15} />Projects {open ? <ChevronDown size={13} /> : <ChevronRight size={13} />}</button>{open && <div className="nested-children"><button className={active === "Aurora" ? "active" : ""} onClick={() => setActive("Aurora")}><Folder size={14} />Aurora launch</button><button className={active === "Website" ? "active" : ""} onClick={() => setActive("Website")}><Folder size={14} />Website refresh</button><button><FileText size={14} />Briefing.pdf</button></div>}<button><Settings size={15} />Settings</button><div className="nested-user"><span>AM</span><b>Ana Moura</b><small>Free plan</small></div></aside><div className="nested-main"><small>PROJECT / {active.toUpperCase()}</small><h3>{active === "Overview" ? "Good afternoon, Ana." : active}</h3><div className="nested-grid"><i /><i /><i /></div></div></PreviewFrame>; }
