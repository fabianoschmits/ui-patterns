"use client";
import { useState } from "react";
import { Archive, Copy, ExternalLink, MoreHorizontal, Pencil, Trash2 } from "lucide-react";
import type { PatternPreviewProps } from "@/types/pattern";
import { PreviewFrame } from "@/patterns/shared/preview-frame";
export default function ElegantContextMenu(props: PatternPreviewProps) { const [open, setOpen] = useState(true); const [done, setDone] = useState(""); return <PreviewFrame {...props} className="context-demo"><div className="context-workspace"><div className="context-toolbar"><span>3 itens selecionados</span><button onClick={() => setOpen((v) => !v)} aria-label="Abrir menu contextual"><MoreHorizontal size={17} /></button></div><div className="context-row active"><span className="context-thumb" /><b>Project Aurora</b><small>Atualizado há 2h</small></div><div className="context-row"><span className="context-thumb second" /><b>Brand principles</b><small>Atualizado ontem</small></div>{open && <div className="context-popover" role="menu"><button onClick={() => setDone("Renomeado")}><Pencil size={15} />Renomear<kbd>R</kbd></button><button onClick={() => setDone("Duplicado")}><Copy size={15} />Duplicar<kbd>⌘ D</kbd></button><button><ExternalLink size={15} />Abrir em nova aba<kbd>↗</kbd></button><hr /><button><Archive size={15} />Arquivar</button><button className="danger" onClick={() => setDone("Pronto para remover")}><Trash2 size={15} />Mover para lixeira</button></div>}<div className="context-toast" aria-live="polite">{done}</div></div></PreviewFrame>; }
