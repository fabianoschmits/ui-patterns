"use client";
import { useState } from "react";
import { Heart, Minus, Plus, ShoppingBag } from "lucide-react";
import type { PatternPreviewProps } from "@/types/pattern";
import { PreviewFrame } from "@/patterns/shared/preview-frame";
export default function ModernProductCard(props: PatternPreviewProps) { const [tone, setTone] = useState("cream"); const [saved, setSaved] = useState(false); const [qty, setQty] = useState(1); return <PreviewFrame {...props} className="product-demo"><div className="product-card"><div className={"product-art tone-" + tone}><span>atelier<br />no. 04</span><i /></div><div className="product-info"><div><small>TEXTILES / 04</small><h3>Folded linen throw</h3></div><button className={saved ? "active" : ""} onClick={() => setSaved((v) => !v)} aria-label="Favoritar produto"><Heart size={17} fill={saved ? "currentColor" : "none"} /></button><p>Uma peça silenciosa, tecida para durar. Linho lavado, acabamento manual.</p><div className="product-meta"><span><b>€ 128</b><small>ou 3x sem juros</small></span><div className="product-qty"><button onClick={() => setQty(Math.max(1, qty - 1))} aria-label="Diminuir quantidade"><Minus size={12} /></button><span>{qty}</span><button onClick={() => setQty(qty + 1)} aria-label="Aumentar quantidade"><Plus size={12} /></button></div></div><div className="product-actions"><div className="swatches">{["cream", "blue", "mint"].map((item) => <button key={item} className={tone === item ? "active tone-" + item : "tone-" + item} onClick={() => setTone(item)} aria-label={"Selecionar cor " + item} />)}</div><button className="product-buy"><ShoppingBag size={15} /> Adicionar</button></div></div></div></PreviewFrame>; }
