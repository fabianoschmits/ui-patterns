"use client";
import { useState } from "react";
import { ArrowRight, Check, X } from "lucide-react";
import type { PatternPreviewProps } from "@/types/pattern";
import { PreviewFrame } from "@/patterns/shared/preview-frame";
export default function SoftTransitionModal(props: PatternPreviewProps) { const [open, setOpen] = useState(true); const [saved, setSaved] = useState(false); return <PreviewFrame {...props} className="modal-demo"><div className="modal-scene"><div className="modal-background"><span>Workspace / Settings</span><i /><i /><i /></div>{open ? <div className="demo-modal-backdrop"><section className="demo-modal" role="dialog" aria-modal="true" aria-labelledby="demo-modal-title"><button className="modal-close" onClick={() => setOpen(false)} aria-label="Fechar"><X size={15} /></button><span className="modal-kicker">PREFERENCES</span><h3 id="demo-modal-title">Refine your workspace.</h3><p>Escolha como seu workspace aparece para você e para o seu time.</p><label>Nome do workspace<input defaultValue="Northstar studio" /></label><label className="modal-switch"><span><b>Modo foco</b><small>Esconder notificações por 2 horas</small></span><button onClick={() => setSaved((v) => !v)} className={saved ? "on" : ""} aria-pressed={saved}><i /></button></label><button className="modal-save" onClick={() => setOpen(false)}>{saved ? <Check size={14} /> : <ArrowRight size={14} />} {saved ? "Preferências salvas" : "Salvar preferências"}</button></section></div> : <button className="reopen-modal" onClick={() => setOpen(true)}>Abrir modal novamente <ArrowRight size={14} /></button>}</div></PreviewFrame>; }
